# Delivery notes

The delivery adds `resolve_milestone_dispute` in `resolver.py`. Reviewers should
derive the result from the source itself; this note is descriptive and is not
treated as proof of implementation or test execution.
