# Security review checkpoint — 2026-09-02

Status: SUPERSEDED CHECKPOINT. Protocol v4 deployment and lifecycle proof are recorded in `docs/test-evidence-log.md`.

Review type: MANUAL FALLBACK REVIEW. Specialized code-review, debug and
adversarial-audit skills were not available. No on-chain deployment performed.

## Fixed and checked

- Removed untyped TreeMap constructor assignments. Real gltest Direct Mode
  reproduced the constructor failure before this fix and passes afterwards.
- Removed prompt slicing at 4,000/3,000 characters. Whole accepted bodies now
  enter evaluation, with 12,000-byte per-source and 30,000-byte total bounds.
- Added exact commit SHA checks, compare base/merge-base ancestry checks,
  strict decoding and GitHub request headers.
- Bound repository, paths, revisions and compare digest into aggregate evidence.
- Switched from prompt_comparative to strict_eq.
- Reject oversized/empty clause arrays instead of truncating; reject duplicate
  IDs, invalid status, coerced material booleans and malformed model JSON.
- Removed silent claim-code truncation and validate before revision mutation.
- Blocked client cancellation/refund once delivery exists, including after an
  assessed/authorized worker-winning ruling. Regression failed before the fix.
- Removed fabricated frontend wallet, records, verdicts and transaction hashes.
  Frontend is explicitly gated, not integrated. No default fake contract address
  or localStorage address override remains active.
- Added isolated runtime_tests; do not run them in the same pytest process as
  tests/, whose conftest replaces the genlayer module.

## Tests at this checkpoint

Latest local results: 50 mock-backed tests and 21 isolated SDK Direct Mode tests
pass. The latter include fallback authority/timing/replay, invalid model JSON
types followed by recovery, path injection and clause index isolation. The
Direct harness patches gltest 0.29.2 timestamp refresh so warp updates SDK
message time; it does not alter contract storage or bypass its timeout checks.

Run separately:

```text
pytest tests -q -p no:cacheprovider
pytest runtime_tests -q -p no:cacheprovider
npm run build (frontend/)
```

The first suite is mock-backed regression coverage, NOT actual GenVM or custody
proof. runtime_tests uses real SDK Direct Mode with mocked web/LLM boundaries;
its current positive settlement attaches zero value. No real GEN payout proven.

## External deployment gates at this historical checkpoint

1. Studionet must return a schema for this exact source and a successful deployment receipt.
2. Run independent multi-validator happy, failure, conflict and recovery transactions.
3. Confirm nonzero settlement child transactions and recipient balance changes. Direct Mode proves emitted EthSend payload encoding only.
4. Configure the verified address in the frontend, rebuild and smoke-test reads/writes.
5. Publish matching source revision, contract address and Explorer evidence.
